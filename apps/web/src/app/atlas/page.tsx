export default function AtlasPage() {
  return (
    <main className="mx-auto max-w-6xl px-6 py-16">
      <h1 className="font-display text-4xl font-semibold tracking-tight">Atlas</h1>
      <p className="mt-4 max-w-xl text-muted">
        The Cape Town gallery map will live here. Galleries render as nodes on a
        stripped-back map, with a card for each gallery beside it.
      </p>
    </main>
  );
}
